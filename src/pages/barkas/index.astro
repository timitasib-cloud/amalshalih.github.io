---
import BaseLayout from '@layouts/BaseLayout.astro'
import {
	barkasCategories as categories,
	barkasProgramInfo as programInfo,
	resolveBarkasProducts,
} from '@lib/barkas'

const products = await resolveBarkasProducts()

// Filter hanya produk yang available
const availableProducts = products.filter((p) => p.status === 'available')

// Schema.org untuk Google Merchant
const organizationSchema = {
	'@context': 'https://schema.org',
	'@type': 'Organization',
	name: 'Yayasan Amal Shalih Insan Bantul',
	url: 'https://amalshalih.or.id',
	logo: 'https://amalshalih.or.id/images/logo.png',
	description: programInfo.description,
	contactPoint: {
		'@type': 'ContactPoint',
		telephone: programInfo.contact.whatsapp,
		contactType: 'customer service',
		availableLanguage: 'Indonesian',
	},
	address: {
		'@type': 'PostalAddress',
		addressLocality: 'Bantul',
		addressRegion: 'Yogyakarta',
		addressCountry: 'ID',
	},
}

const productSchema = {
	'@context': 'https://schema.org',
	'@type': 'ItemList',
	itemListElement: availableProducts.map((product, index) => ({
		'@type': 'ListItem',
		position: index + 1,
		item: {
			'@type': 'Product',
			name: product.name,
			description: product.description,
			image: product.images.map((img) =>
				/^(https?:)?\/\//i.test(img) ? img : `https://amalshalih.or.id${img}`,
			),
			offers: {
				'@type': 'Offer',
				price: product.price,
				priceCurrency: 'IDR',
				availability:
					product.status === 'available'
						? 'https://schema.org/InStock'
						: 'https://schema.org/OutOfStock',
				condition: 'https://schema.org/UsedCondition',
				seller: {
					'@type': 'Organization',
					name: 'Yayasan Amal Shalih Insan Bantul',
				},
			},
		},
	})),
}
---

<BaseLayout title={`${programInfo.fullName} - ${programInfo.tagline}`} description={programInfo.description} image="/dampak-nyata.webp" currentPage="/barkas">
  <!-- Schema.org JSON-LD -->
  <script is:inline type="application/ld+json" set:html={JSON.stringify(organizationSchema)} />
  <script is:inline type="application/ld+json" set:html={JSON.stringify(productSchema)} />

  <main class="barkas-page">
    <!-- ==================== HERO ==================== -->
    <section class="barkas-hero -mt-[70px] sm:-mt-[82px] min-h-[300px] sm:min-h-[320px] lg:min-h-[360px]">
      <div class="hero-overlay" />
      <div class="container pt-[90px] sm:pt-[110px] pb-6 sm:pb-10 lg:pb-12">
        <div class="hero-content animate-fade-in">
          <h1>{programInfo.fullName}</h1>
          <p class="tagline animate-fade-in-up delay-1">{programInfo.tagline}</p>
          <p class="description animate-fade-in-up delay-2">{programInfo.description}</p>

          <div class="hero-cta animate-fade-in-up delay-3">
            <a href="/barkas/donasi" class="btn hero-btn-primary">Donasi Barang</a>
            <a href="/barkas/tentang" class="btn hero-btn-secondary">Tentang BARKAS</a>
          </div>
        </div>
      </div>
    </section>

    <!-- ==================== KATALOG ==================== -->
    <section class="catalog" id="katalog">
      <div class="container">
        <div class="section-header animate-fade-in-up">
          <h2>Katalog Produk Tersedia</h2>
          <p class="catalog-description">
            Semua hasil penjualan barang bekas ini digunakan untuk mendukung program sosial, pendidikan, dan dakwah Yayasan ASIB.
          </p>
        </div>

        <!-- Category Filter -->
        <div class="category-filter animate-fade-in-up delay-1">
          <button class="filter-btn active" data-category="all">Semua</button>
          {categories.map(cat => (
            <button class="filter-btn" data-category={cat.id}>{cat.name}</button>
          ))}
        </div>

    <!-- Product Grid -->
    <div class="product-grid" id="product-grid">
          {availableProducts.map((product) => {
            const categoryName = categories.find(c => c.id === product.category)?.name || '';
            return (
            <article class="product-card" data-category={product.category}>
    <div class="product-image">
        <img 
            src={product.images[0]} 
            alt={product.name}
            loading="lazy"
            decoding="async"
        />
        {product.originalPrice && (
            <span class="badge badge-discount">
                Hemat {Math.round((1 - product.price / product.originalPrice) * 100)}%
            </span>
        )}
    </div>

              <div class="product-info">
                <h3 class="product-name">{product.name}</h3>
                <p class="product-description">{product.description.substring(0, 100)}...</p>

                <div class="product-meta">
                  <span class="category-tag">{categoryName}</span>
                  <span class="condition-tag">
                    {product.condition === 'used-excellent' && '⭐ Seperti Baru'}
                    {product.condition === 'used-good' && '✓ Kondisi Baik'}
                    {product.condition === 'used-fair' && '◐ Layak Pakai'}
                  </span>
                  <span class="badge-available animate-badge-pulse">Tersedia</span>
                </div>

                <div class="product-pricing">
                  <span class="current-price">Rp {product.price.toLocaleString('id-ID')}</span>
                  {product.originalPrice && (
                    <span class="original-price">Rp {product.originalPrice.toLocaleString('id-ID')}</span>
                  )}
                </div>

                <div class="product-actions">
                  <a 
                    href={`/barkas/${product.id}`}
                    class="btn btn-primary btn-block"
                  >
                    Lihat Detail
                  </a>
                  <a 
                    href={`https://wa.me/${programInfo.contact.whatsapp.replace(/\D/g, '')}?text=Halo%20ASIB,%20saya%20tertarik%20dengan%20${encodeURIComponent(product.name)}`}
                    class="btn btn-whatsapp btn-block"
                    target="_blank"
                    rel="noopener noreferrer"
                  >
                    💬 Chat via WhatsApp
                  </a>
                </div>

                <div class="donor-info">
                  <small>
                    🎁 Donatur: {product.donor}<br/>
                    📅 Diterima: {product.acquiredDate ? new Date(product.acquiredDate).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) : '—'}
                  </small>
                </div>
              </div>
            </article>
            );
          })}
        </div>

        {availableProducts.length === 0 && (
          <div class="empty-state animate-fade-in">
            <p>📦 Belum ada produk tersedia saat ini.</p>
            <p>Hubungi kami untuk info barang masuk berikutnya!</p>
          </div>
        )}
      </div>
    </section>

    <!-- ==================== CTA BANNER ==================== -->
    <section class="cta-banner animate-fade-in-up">
      <div class="container">
        <div class="cta-content">
          <h2>Ingin Berkontribusi?</h2>
          <p>Ada barang bekas yang masih layak pakai? Donasikan untuk program sosial ASIB.</p>
          <div class="cta-links">
            <a href="/barkas/donasi" class="btn btn-primary btn-large">Donasi Barang</a>
            <a href="/barkas/dampak" class="btn btn-outline btn-large">Lihat Dampak</a>
          </div>
        </div>
      </div>
    </section>
  </main>

  <style>
    .barkas-page {
      font-family: system-ui, -apple-system, sans-serif;
    }

    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding-left: 20px;
      padding-right: 20px;
    }

    /* ==============================
       HERO
    ============================== */
    .barkas-hero {
      position: relative;
      background-image: url('https://images.unsplash.com/photo-1556742049-0cfed4f6a45d?w=1400&q=75');
      background-size: cover;
      background-position: center 40%;
      color: white;
      text-align: center;
      overflow: hidden;
    }

    .hero-overlay {
      position: absolute;
      top: -70px;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(0,50,15,0.88) 0%, rgba(0,30,10,0.75) 50%, rgba(0,0,0,0.5) 100%);
      pointer-events: none;
    }
    
    @media (min-width: 640px) {
      .hero-overlay {
        top: -82px;
      }
    }

    .hero-content {
      position: relative;
      z-index: 1;
    }

    .barkas-hero h1 {
      font-size: 2.5rem;
      margin-bottom: 10px;
      font-weight: 700;
      color: white;
    }

    .tagline {
      font-size: 1.5rem;
      margin-bottom: 20px;
      opacity: 0.95;
      color: white;
    }

    .description {
      font-size: 1.1rem;
      max-width: 800px;
      margin: 0 auto 30px;
      opacity: 0.95;
      color: rgba(255, 255, 255, 0.95);
    }

    .hero-cta {
      display: flex;
      gap: 15px;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* ==============================
       BUTTONS
    ============================== */
    .btn {
      padding: 12px 24px;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      display: inline-block;
      border: none;
      cursor: pointer;
    }

    .btn:active {
      transform: scale(0.97) !important;
    }

    /* Hero-specific buttons (transparent glass on hero background) */
    .hero-btn-primary {
      background: rgba(255, 255, 255, 0.95);
      color: var(--color-primary-700);
    }

    .hero-btn-primary:hover {
      background: rgba(255, 255, 255, 1);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
    }

    .hero-btn-secondary {
      background: rgba(255, 255, 255, 0.15);
      color: white;
      border: 2px solid rgba(255, 255, 255, 0.8);
      backdrop-filter: blur(4px);
    }

    .hero-btn-secondary:hover {
      background: rgba(255, 255, 255, 0.25);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }

    /* Standard primary button: solid green — shared with [id].astro */
    .btn-primary {
      background: var(--color-accent-primary);
      color: white;
      border: none;
    }

    .btn-primary:hover {
      background: var(--color-primary-800);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px var(--color-shadow-accent-hover);
    }

    /* Standard secondary button: outlined — shared with [id].astro */
    .btn-secondary {
      background: var(--color-surface-elevated);
      color: var(--color-accent-primary);
      border: 2px solid var(--color-accent-primary);
    }

    .btn-secondary:hover {
      background: var(--color-surface-hover);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px var(--color-shadow-elevated-hover);
    }

    .btn-outline {
      background: transparent;
      color: var(--color-accent-primary);
      border: 2px solid var(--color-accent-primary);
    }

    .btn-outline:hover {
      background: var(--color-accent-primary);
      color: white;
      transform: translateY(-2px);
    }

    .btn-block {
      width: 100%;
      text-align: center;
    }

    .btn-large {
      padding: 15px 30px;
      font-size: 1.1rem;
    }

    .btn-whatsapp {
      background: var(--color-whatsapp, #25D366);
      color: white;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .btn-whatsapp:hover {
      background: var(--color-whatsapp-dark, #128C7E);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(37, 211, 102, 0.3);
    }

    /* ==============================
       CATALOG
    ============================== */
    .catalog {
      padding: 60px 0;
      background: var(--color-surface-secondary);
    }

    .section-header {
      text-align: center;
      margin-bottom: 10px;
    }

    .catalog h2 {
      text-align: center;
      font-size: 2rem;
      margin-bottom: 10px;
      color: var(--color-text-primary);
    }

    .catalog-description {
      text-align: center;
      color: var(--color-text-secondary);
      margin-bottom: 30px;
    }

    .category-filter {
      display: flex;
      gap: 10px;
      justify-content: center;
      flex-wrap: wrap;
      margin-bottom: 30px;
    }

    .filter-btn {
      padding: 8px 20px;
      border: 2px solid var(--color-accent-primary);
      background: var(--color-surface-elevated);
      color: var(--color-accent-primary);
      border-radius: 20px;
      cursor: pointer;
      font-weight: 600;
      transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .filter-btn:hover {
      background: var(--color-accent-primary);
      color: white;
      transform: translateY(-1px);
    }

    .filter-btn:active {
      transform: scale(0.95);
    }

    .filter-btn.active {
      background: var(--color-accent-primary);
      color: white;
      box-shadow: 0 4px 12px rgba(22, 163, 74, 0.3);
    }

    .product-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 30px;
    }

    /* Mobile grid override — explicit 2 columns for native feel */
    @media (max-width: 639px) {
      .product-grid {
        grid-template-columns: repeat(2, 1fr);
        gap: 8px;
      }
    }
    /* Tablet grid override — explicit 3 columns */
    @media (min-width: 640px) and (max-width: 1023px) {
      .product-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 16px;
      }
    }

    .product-card {
      background: var(--color-surface-elevated);
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      border: 1px solid transparent;
    }

    .product-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 16px 40px rgba(0, 0, 0, 0.12), 0 4px 12px rgba(0, 0, 0, 0.06);
      border-color: var(--color-border-primary);
    }

    .product-image {
      position: relative;
      height: 250px;
      overflow: hidden;
      background: var(--color-surface-placeholder);
    }

    .product-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      transition: transform 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .product-card:hover .product-image img {
      transform: scale(1.08);
    }

    .badge-discount {
      position: absolute;
      top: 10px;
      right: 10px;
      background: var(--color-red-600);
      color: white;
      padding: 5px 12px;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: bold;
      box-shadow: 0 2px 8px rgba(220, 38, 38, 0.4);
      animation: cta-pulse 2s ease-in-out infinite;
    }

    .product-info {
      padding: 20px;
    }

    .product-name {
      font-size: 1.1rem;
      margin-bottom: 10px;
      color: var(--color-text-primary);
      transition: color 0.3s;
    }

    .product-card:hover .product-name {
      color: var(--color-accent-primary);
    }

    .product-description {
      color: var(--color-text-secondary);
      font-size: 0.9rem;
      margin-bottom: 15px;
      line-height: 1.5;
    }

    .product-meta {
      display: flex;
      gap: 8px;
      margin-bottom: 15px;
      flex-wrap: wrap;
      align-items: center;
    }

    .category-tag,
    .condition-tag {
      padding: 4px 10px;
      border-radius: 12px;
      font-size: 0.8rem;
      font-weight: 600;
      transition: transform 0.3s;
    }

    .category-tag:hover,
    .condition-tag:hover {
      transform: scale(1.05);
    }

    .category-tag {
      background: var(--color-badge-primary-bg);
      color: var(--color-badge-primary-text);
    }

    .condition-tag {
      background: var(--color-badge-gold-bg);
      color: var(--color-badge-gold-text);
    }

    .badge-available {
      display: inline-block;
      background: var(--color-badge-primary-bg);
      color: var(--color-badge-primary-text);
      padding: 3px 10px;
      border-radius: 12px;
      font-size: 0.75rem;
      font-weight: 700;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }

    .product-pricing {
      margin-bottom: 15px;
    }

    .current-price {
      font-size: 1.5rem;
      font-weight: bold;
      color: var(--color-accent-primary);
    }

    .original-price {
      font-size: 1rem;
      color: var(--color-text-muted);
      text-decoration: line-through;
      margin-left: 10px;
    }

    .product-actions {
      display: flex;
      flex-direction: column;
      gap: 10px;
      margin-bottom: 15px;
    }

    .donor-info {
      padding-top: 15px;
      border-top: 1px solid var(--color-border-secondary);
      color: var(--color-text-secondary);
      font-size: 0.85rem;
    }

    .empty-state {
      text-align: center;
      padding: 60px 20px;
      color: var(--color-text-secondary);
    }

    /* ==============================
       CTA BANNER
    ============================== */
    .cta-banner {
      padding: 60px 0;
      background: var(--color-surface);
    }

    .cta-content {
      text-align: center;
      max-width: 600px;
      margin: 0 auto;
    }

    .cta-content h2 {
      font-size: 2rem;
      color: var(--color-text-primary);
      margin-bottom: 15px;
    }

    .cta-content p {
      color: var(--color-text-secondary);
      margin-bottom: 30px;
      font-size: 1.1rem;
    }

    .cta-links {
      display: flex;
      gap: 15px;
      justify-content: center;
      flex-wrap: wrap;
    }

    /* ==============================
       SHIMMER SKELETON
    ============================== */
    .skeleton-card {
      background: var(--color-surface-elevated);
      border-radius: 12px;
      overflow: hidden;
    }

    .skeleton-image {
      height: 250px;
      background: linear-gradient(
        90deg,
        var(--color-surface-placeholder) 25%,
        var(--color-surface-elevated) 50%,
        var(--color-surface-placeholder) 75%
      );
      background-size: 200% 100%;
      animation: shimmer 1.5s ease-in-out infinite;
    }

    .skeleton-line {
      height: 14px;
      margin: 15px 20px;
      border-radius: 8px;
      background: linear-gradient(
        90deg,
        var(--color-surface-placeholder) 25%,
        var(--color-surface-elevated) 50%,
        var(--color-surface-placeholder) 75%
      );
      background-size: 200% 100%;
      animation: shimmer 1.5s ease-in-out infinite;
    }

    .skeleton-line.short {
      width: 60%;
    }

    .skeleton-line.medium {
      width: 80%;
    }

    /* ==============================
       RESPONSIVE
    ============================== */
    /* ==============================
       MOBILE NATIVE APP STYLES (≤768px)
       Tokopedia/Shopee-inspired compact cards
    ============================== */
    @media (max-width: 768px) {
    /* ── Grid ── */
    .product-grid {
      gap: 8px;
    }

    /* ── Card: flat, no shadow, minimal border ── */
    .product-card {
      border-radius: 6px;
      box-shadow: none !important;
      border: none;
    }
    .product-card:hover {
      transform: none !important;
      box-shadow: none !important;
      border-color: transparent !important;
    }

    /* ── Image: 1:1 square (Tokopedia style) ── */
    .product-image {
      height: auto !important;
      aspect-ratio: 1 / 1;
    }
    .product-image img {
      position: absolute;
      inset: 0;
    }
    .product-card:hover .product-image img {
      transform: none;
    }

    /* ── Discount badge: top-left overlay, compact ── */
    .badge-discount {
      top: 4px;
      left: 4px;
      right: auto;
      padding: 2px 6px;
      font-size: 0.6rem;
      border-radius: 3px;
      box-shadow: none;
      animation: none;
    }

    /* ── Info section: ultra compact ── */
    .product-info {
      padding: 8px 8px;
      display: flex;
      flex-direction: column;
      gap: 2px;
    }

    .product-description {
      display: none !important;
    }

    .product-meta {
      display: none !important;
    }

    .product-name {
      font-size: 0.78rem;
      font-weight: 500;
      margin-bottom: 0;
      line-height: 1.3;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
      overflow: hidden;
      color: var(--color-text-primary);
    }
    .product-card:hover .product-name {
      color: var(--color-text-primary);
    }

    /* ── Pricing: tight rows ── */
    .product-pricing {
      margin-bottom: 0;
      display: flex;
      align-items: baseline;
      gap: 4px;
      flex-wrap: wrap;
    }
    .current-price {
      font-size: 0.8rem;
      font-weight: 600;
      color: var(--color-accent-primary);
    }
    .original-price {
      font-size: 0.6rem;
      margin-left: 0;
    }

    /* ── Actions: WhatsApp hidden, "Lihat Detail" as invisible overlay ── */
    .product-actions {
      position: static;
      margin-bottom: 0;
    }
    .product-actions .btn-whatsapp {
      display: none !important;
    }
    .product-actions .btn-primary {
      position: absolute;
      inset: 0;
      opacity: 0;
      padding: 0;
      z-index: 1;
      width: 100%;
      height: 100%;
      border-radius: 0;
      background: transparent;
    }

    .donor-info {
      display: none !important;
    }

    /* ── Hero ultra compact ── */
    .barkas-hero {
      min-height: 120px !important;
    }
    .barkas-hero h1 {
      font-size: 1.1rem !important;
      margin-bottom: 4px !important;
    }
    .tagline {
      font-size: 0.8rem !important;
      margin-bottom: 4px !important;
    }
    .description {
      font-size: 0.7rem !important;
      margin-bottom: 8px !important;
      max-width: 100% !important;
    }
    .hero-cta {
      gap: 6px !important;
    }
    .hero-content {
      padding-top: 55px !important;
    }
    .hero-btn-secondary {
      display: none !important;
    }
    .hero-btn-primary {
      font-size: 0.8rem !important;
      padding: 8px 16px !important;
    }

    /* ── Category filter: sticky horizontal scroll ── */
    .category-filter {
      overflow-x: auto;
      flex-wrap: nowrap;
      justify-content: flex-start;
      padding: 8px 16px;
      gap: 6px;
      margin-bottom: 8px;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
      position: sticky;
      top: 0;
      z-index: 20;
      background: var(--color-surface-secondary);
      border-bottom: 1px solid var(--color-border-secondary, #e5e7eb);
    }
    .category-filter::-webkit-scrollbar {
      display: none;
    }
    .filter-btn {
      padding: 5px 12px;
      font-size: 0.75rem;
      white-space: nowrap;
      flex-shrink: 0;
    }

    /* ── Spacing ── */
    .catalog {
      padding: 12px 0 !important;
    }
    .catalog h2 {
      font-size: 1rem !important;
      margin-bottom: 2px !important;
    }
    .catalog-description {
      font-size: 0.75rem !important;
      margin-bottom: 6px !important;
    }
    .section-header {
      margin-bottom: 0 !important;
    }
    .cta-banner {
      padding: 16px 0 !important;
    }
    .cta-content h2 {
      font-size: 1.1rem !important;
      margin-bottom: 6px !important;
    }
    .cta-content p {
      font-size: 0.8rem !important;
      margin-bottom: 12px !important;
    }
    .cta-links {
      gap: 8px !important;
    }
    .cta-links .btn {
      padding: 10px 20px !important;
      font-size: 0.85rem !important;
    }

    /* ── Container: balanced 8px for grid alignment ── */
    .container {
      padding-left: 8px !important;
      padding-right: 8px !important;
    }
    /* Keep consistent padding on all content sections */
    .section-header {
      padding-left: 8px;
      padding-right: 8px;
    }
    .cta-content {
      padding-left: 8px;
      padding-right: 8px;
    }
    .empty-state {
      padding-left: 8px;
      padding-right: 8px;
    }
    /* Override hero container padding (needs more) */
    .barkas-hero .container {
      padding-left: 16px !important;
      padding-right: 16px !important;
    }

    /* ── Card wrapper needs relative for overlay ── */
    .product-card {
      position: relative;
    }

    /* ── Tablet override inside mobile — 3 cols on wider phones ── */
    @media (min-width: 640px) {
      .product-grid {
        grid-template-columns: repeat(3, 1fr);
        gap: 12px;
      }
      .product-image {
        aspect-ratio: 1 / 1;
      }
    }
    }

    /* Tablet overrides (640px - 1023px) */
    @media (min-width: 640px) and (max-width: 1023px) {
    .product-image {
      height: 180px !important;
    }
    .product-card:hover {
      transform: translateY(-4px) !important;
    }
    }

    :global(.dark) .btn-outline,
    :global(.dark) .btn-secondary {
      color: var(--color-text-primary);
      border-color: var(--color-text-primary);
    }

    :global(.dark) .btn-outline:hover,
    :global(.dark) .btn-secondary:hover {
      background: var(--color-text-primary);
      color: var(--color-surface);
    }
  </style>

  <script is:inline>
    document.addEventListener('DOMContentLoaded', () => {
      const filterBtns = document.querySelectorAll('.filter-btn');
      const productCards = document.querySelectorAll('.product-card');

      // Staggered entrance animation
      productCards.forEach((card, index) => {
        const el = card;
        const delay = Math.min(index + 1, 6);
        el.style.opacity = '0';
        el.style.animation = `fade-in-up 0.6s ease-out ${delay * 0.12}s forwards`;
      });

      filterBtns.forEach(btn => {
        const b = btn;
        b.addEventListener('click', () => {
          filterBtns.forEach(x => x.classList.remove('active'));
          b.classList.add('active');

          const category = b.dataset.category;

          productCards.forEach((card, index) => {
            const el = card;
            if (category === 'all' || el.dataset.category === category) {
              el.style.display = 'block';
              el.style.animation = 'none';
              void el.offsetWidth;
              const delay = Math.min(index + 1, 6);
              el.style.animation = `fade-in-up 0.5s ease-out ${delay * 0.1}s forwards`;
              el.style.opacity = '0';
            } else {
              el.style.display = 'none';
            }
          });
        });
      });

      // Skeleton when no products
      const grid = document.getElementById('product-grid');
      if (grid && grid.children.length === 0) {
        for (let i = 0; i < 3; i++) {
          const skeleton = document.createElement('div');
          skeleton.className = 'skeleton-card';
          skeleton.innerHTML = `
            <div class="skeleton-image"></div>
            <div class="skeleton-line short"></div>
            <div class="skeleton-line"></div>
            <div class="skeleton-line medium"></div>
          `;
          grid.appendChild(skeleton);
        }
      }
    });
  </script>
</BaseLayout>
